id: drupalcenter
label: "DrupalCenter"
description: "Migrate content from old site."
source_type: mixed sources
shared_configuration:
  source:
    # Set database key for all migrations.
    key: dc_migrate
dependencies:
  enforced:
    module:
      - dc_migrate
